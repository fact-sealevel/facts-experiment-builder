# FAIR MODULE CONFIGURATION
# This YAML file describes the arguments and configuration needed to run the module.
# It is used by the generic module system to generate Docker Compose services.

module_name: "fair"
container_image: "ghcr.io/fact-sealevel/fair-temperature:0.2.1"
climate_file_required: false

# Argument specifications
arguments:
  # Top-level metadata arguments (inherited from experiment metadata)
  top_level:
    - name: "pipeline-id"
      type: "str"
      source: "metadata.pipeline-id"
      help: "Pipeline ID"
    - name: "nsamps"
      type: "int"
      source: "metadata.nsamps"
      help: "Number of samples"
    - name: "seed"
      type: "int"
      source: "metadata.seed"
      help: "Random seed to use for sampling"
    - name: "scenario"
      type: "str"
      source: "metadata.scenario"
      transform: "scenario_name"  # Extract from ScenarioConfig object
      help: "Emissions scenario name"
  
  # Module-specific options (temperature module options)
  options:
    - name: "cyear-start"
      type: "int"
      source: "module_inputs.inputs.cyear_start"
      help: "TO DO ADD HELP TEXT"
    - name: "cyear-end"
      type: "int"
      source: "module_inputs.inputs.cyear_end"
      help: "TO DO ADD HELP TEXT"
    - name: "smooth-win"
      type: "int"
      source: "module_inputs.inputs.smooth_win"
      help: "TO DO ADD HELP TEXT"
  inputs:
    - name: "rcmip-file"
      type: "file"
      source: "module_inputs.inputs.rcmip_fname"
      help: "TO DO ADD HELP TEXT"
      mount:
        volume: "input"
        container_path: "/mnt/module_specific_in"
    - name: "param-file"
      type: "file"
      source: "module_inputs.inputs.param_fname"
      help: "TO DO ADD HELP TEXT"
      mount:
        volume: "input"
        container_path: "/mnt/module_specific_in"
  # Input file arguments

  
  # Output file arguments
  # Note: outputs are mapped by index from the metadata outputs list
  outputs:
    - name: "output-oceantemp-file"
      type: "file"
      source: "module_inputs.outputs.output_0"  # First output (oceantemp)
      help: "TO DO ADD HELP TEXT"
      mount:
        volume: "output"
        container_path: "/mnt/out"
        transform: "filename"
    - name: "output-climate-file"
      type: "file"
      source: "module_inputs.outputs.output_1"  # Second output (climate)
      help: "TO DO ADD HELP TEXT"
      mount:
        volume: "output"
        container_path: "/mnt/out"
        transform: "filename"
    - name: "output-ohc-file"
      type: "file"
      source: "module_inputs.outputs.output_2"  # Third output (ohc)
      help: "TO DO ADD HELP TEXT"
      mount:
        volume: "output"
        container_path: "/mnt/out"
        transform: "filename"
    - name: "output-gsat-file"
      type: "file"
      source: "module_inputs.outputs.output_3"  # Fourth output (gsat)
      help: "TO DO ADD HELP TEXT"
      mount:
        volume: "output"
        container_path: "/mnt/out"
        transform: "filename"

# Volume specifications (derived from input/output paths)
volumes:
  input:
    host_path: "module_inputs.input_paths.input_dir"
    help: "TO DO ADD HELP TEXT"
    container_path: "/mnt/module_specific_in"
  output:
    host_path: "module_inputs.output_paths.output_dir"
    help: "TO DO ADD HELP TEXT"
    container_path: "/mnt/out"

# No service dependencies (fair is typically the first module)
