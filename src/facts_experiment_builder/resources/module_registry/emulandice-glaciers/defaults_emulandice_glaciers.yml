# Default values for the EMULANDICE GLACIERS module command (glaciers)
# These values are used when creating new experiments and can be overridden in experiment metadata

# internal note: Location file defaults DO NOT belong here.

# Module-specific options
options:
  chunksize: 50

fingerprint_params:
  fprint-glacier-dir: "fprint_glacier"
  fprint-map-file: "fprint_map.csv"
