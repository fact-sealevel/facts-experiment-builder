# Default values for the OELSMANN24 VERTICALLANDMOTION module
# These values are used when creating new experiments and can be overridden in experiment metadata

# internal note: Image information and Location file defaults DO NOT belong here.

# Module-specific options
options:
  chunk_size: 50

# Input paths
inputs:
  vlm_reconstruction_fname: "VLM_reconstruction.nc"
  gps_vlm_fname: "NGL14_CMR_corrected.nc"
  gia_vlm_fname: "GIA_stats.nc"
  dist2coast_fname: "dist2coast_1deg_v2.grd"
