# Default values for the FittedISMIP GRIS module
# These values are used when creating new experiments and can be overridden in experiment metadata

# internal note: Image information andLocation file defaults DO NOT belong here.

options:
  no_drift_corr: false
  no_correlation: false
  scenario_dsl: ""
fingerprint_params:
  fingerprint-dir: FPRINT
inputs:
  expansion_coefficients_file: scmpy2LM_RCMIP_CMIP6calpm_n18_expcoefs.nc
  gsat_rmses_file: scmpy2LM_RCMIP_CMIP6calpm_n17_gsat_rmse.nc
  model_dir: cmip6/
